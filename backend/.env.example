# Mongo
MONGO_URI=mongouri
NODE_ENV=development
PORT=5000

# JWT
JWT_ACCESS_SECRET=secretoseguro
JWT_REFRESH_SECRET=secretosegurorefresh
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# Bcrypt
BCRYPT_SALT_ROUNDS=10

# CORS
ALLOWED_ORIGINS=http://localhost:3000,https://tudominio.com

# Rate limiting (auth)
RATE_LIMIT_WINDOW_MS=900000 # 15 minutos
RATE_LIMIT_MAX=100          # 100 req/ventana